Make sure you have the following R packages installed:

* shiny
* leaflet (<code>devtools::install_github('rstudio/leaflet')</code>)
* sp

Then just run:

<code>shiny::runGitHub(repo="FlatOutGovHack2015/FlatOutShiny")</code>
